<!doctype html>
<html>
  <head>
    <meta charset="utf-8"/>
    <title>${title}</title>
    <style>
      body { font-family: system-ui, sans-serif; background: ${bg}; color: ${fg}; }
      main { max-width: 42rem; margin: 10vh auto; line-height: 1.6; }
      code { padding: 2px 4px; border: 1px solid #ccc; }
    </style>
  </head>
  <body>
    <main>
      <h1>${title}</h1>
      <p>Ciao da <code>OpenTofu + Docker</code>. Porta esposta: <strong>${port}</strong>.</p>
      <p>Tag immagine: <strong>${tag}</strong>. Container: <strong>${name}</strong>.</p>
    </main>
  </body>
</html>
